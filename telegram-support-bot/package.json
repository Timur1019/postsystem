{
  "name": "telegram-support-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Telegram-бот поддержки Aurent POS — принимает обращения и пересылает в группу",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "dev": "node --env-file=.env --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "telegraf": "^4.16.3"
  }
}
